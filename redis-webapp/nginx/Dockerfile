# Use the official Nginx image from the Docker Hub
FROM nginx:1.24-alpine

# Copy the local index.html file to the Nginx default web directory
COPY index.html /usr/share/nginx/html/

# Expose port 80 to the outside world (default Nginx port)
EXPOSE 80
